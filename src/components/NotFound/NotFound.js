import React, { Component } from 'react'
import './NotFound.scss';

export default class NotFound extends Component {
    render() {
        return (
            <div id="notFound">
                <p>Désolé, cette page n'existe pas, plus, ou pas encore.</p>
            </div>
        )
    }
}
